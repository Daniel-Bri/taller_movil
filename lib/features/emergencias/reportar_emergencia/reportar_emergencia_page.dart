import 'package:flutter/material.dart';

// CU05 - Reportar Emergencia
class ReportarEmergenciaPage extends StatelessWidget {
  const ReportarEmergenciaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Reportar Emergencia')),
      body: const Center(child: Text('CU05 - Reportar Emergencia')),
    );
  }
}
