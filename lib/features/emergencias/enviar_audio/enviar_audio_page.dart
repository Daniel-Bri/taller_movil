import 'package:flutter/material.dart';

// CU08 - Enviar Audio
class EnviarAudioPage extends StatelessWidget {
  const EnviarAudioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Enviar Audio')),
      body: const Center(child: Text('CU08 - Enviar Audio')),
    );
  }
}
