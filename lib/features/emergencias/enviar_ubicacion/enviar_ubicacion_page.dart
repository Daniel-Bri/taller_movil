import 'package:flutter/material.dart';

// CU06 - Enviar Ubicación GPS
class EnviarUbicacionPage extends StatelessWidget {
  const EnviarUbicacionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Enviar Ubicación GPS')),
      body: const Center(child: Text('CU06 - Enviar Ubicación GPS')),
    );
  }
}
