import 'package:flutter/material.dart';

// CU09 - Agregar Descripción
class AgregarDescripcionPage extends StatelessWidget {
  const AgregarDescripcionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Agregar Descripción')),
      body: const Center(child: Text('CU09 - Agregar Descripción')),
    );
  }
}
