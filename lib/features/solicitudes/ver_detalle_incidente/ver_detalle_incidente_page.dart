import 'package:flutter/material.dart';

// CU14 - Ver Detalle del Incidente
class VerDetalleIncidentePage extends StatelessWidget {
  const VerDetalleIncidentePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Ver Detalle del Incidente')),
      body: const Center(child: Text('CU14 - Ver Detalle del Incidente')),
    );
  }
}
