import 'package:flutter/material.dart';

// CU13 - Ver Solicitudes Disponibles
class VerSolicitudesDisponiblesPage extends StatelessWidget {
  const VerSolicitudesDisponiblesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Ver Solicitudes Disponibles')),
      body: const Center(child: Text('CU13 - Ver Solicitudes Disponibles')),
    );
  }
}
