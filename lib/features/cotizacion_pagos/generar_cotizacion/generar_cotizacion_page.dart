import 'package:flutter/material.dart';

// CU23 - Generar Cotización
class GenerarCotizacionPage extends StatelessWidget {
  const GenerarCotizacionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Generar Cotización')),
      body: const Center(child: Text('CU23 - Generar Cotización')),
    );
  }
}
