import 'package:flutter/material.dart';

// CU32 - Ver Comisiones
class VerComisionesPage extends StatelessWidget {
  const VerComisionesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Ver Comisiones')),
      body: const Center(child: Text('CU32 - Ver Comisiones')),
    );
  }
}
