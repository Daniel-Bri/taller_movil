import 'package:flutter/material.dart';

// CU25 - Confirmar Cotización
class ConfirmarCotizacionPage extends StatelessWidget {
  const ConfirmarCotizacionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Confirmar Cotización')),
      body: const Center(child: Text('CU25 - Confirmar Cotización')),
    );
  }
}
