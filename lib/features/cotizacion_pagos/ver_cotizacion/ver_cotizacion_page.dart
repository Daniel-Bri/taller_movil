import 'package:flutter/material.dart';

// CU24 - Ver Cotización
class VerCotizacionPage extends StatelessWidget {
  const VerCotizacionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Ver Cotización')),
      body: const Center(child: Text('CU24 - Ver Cotización')),
    );
  }
}
