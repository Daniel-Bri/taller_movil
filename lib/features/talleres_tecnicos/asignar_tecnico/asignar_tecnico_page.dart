import 'package:flutter/material.dart';

// CU17 - Asignar Técnico
class AsignarTecnicoPage extends StatelessWidget {
  const AsignarTecnicoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Asignar Técnico')),
      body: const Center(child: Text('CU17 - Asignar Técnico')),
    );
  }
}
