import 'package:flutter/material.dart';

// CU30 - Gestionar Técnicos
class GestionarTecnicosPage extends StatelessWidget {
  const GestionarTecnicosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Gestionar Técnicos')),
      body: const Center(child: Text('CU30 - Gestionar Técnicos')),
    );
  }
}
