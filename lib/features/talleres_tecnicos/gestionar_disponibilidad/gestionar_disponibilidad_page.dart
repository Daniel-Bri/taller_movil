import 'package:flutter/material.dart';

// CU19 - Gestionar Disponibilidad
class GestionarDisponibilidadPage extends StatelessWidget {
  const GestionarDisponibilidadPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Gestionar Disponibilidad')),
      body: const Center(child: Text('CU19 - Gestionar Disponibilidad')),
    );
  }
}
