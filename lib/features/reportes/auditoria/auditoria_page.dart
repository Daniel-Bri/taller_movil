import 'package:flutter/material.dart';

// CU36 - Ver Auditoría del Sistema
class AuditoriaPage extends StatelessWidget {
  const AuditoriaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Ver Auditoría del Sistema')),
      body: const Center(child: Text('CU36 - Ver Auditoría del Sistema')),
    );
  }
}
