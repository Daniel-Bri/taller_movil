import 'package:flutter/material.dart';

// CU35 - Ver Métricas Globales
class MetricasGlobalesPage extends StatelessWidget {
  const MetricasGlobalesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Ver Métricas Globales')),
      body: const Center(child: Text('CU35 - Ver Métricas Globales')),
    );
  }
}
