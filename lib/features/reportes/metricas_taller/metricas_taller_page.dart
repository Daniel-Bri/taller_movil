import 'package:flutter/material.dart';

// CU31 - Ver Métricas del Taller
class MetricasTallerPage extends StatelessWidget {
  const MetricasTallerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Ver Métricas del Taller')),
      body: const Center(child: Text('CU31 - Ver Métricas del Taller')),
    );
  }
}
