import 'package:flutter/material.dart';

// CU28 - Calificar Servicio
class CalificarServicioPage extends StatelessWidget {
  const CalificarServicioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Calificar Servicio')),
      body: const Center(child: Text('CU28 - Calificar Servicio')),
    );
  }
}
