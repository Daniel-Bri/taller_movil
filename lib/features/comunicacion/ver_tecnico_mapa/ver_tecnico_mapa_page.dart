import 'package:flutter/material.dart';

// CU20 - Ver Técnico en Mapa
class VerTecnicoMapaPage extends StatelessWidget {
  const VerTecnicoMapaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Ver Técnico en Mapa')),
      body: const Center(child: Text('CU20 - Ver Técnico en Mapa')),
    );
  }
}
