import 'package:flutter/material.dart';

// CU34 - Aprobar Talleres
class AprobarTalleresPage extends StatelessWidget {
  const AprobarTalleresPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Aprobar Talleres')),
      body: const Center(child: Text('CU34 - Aprobar Talleres')),
    );
  }
}
