import 'package:flutter/material.dart';

// CU04 - Gestionar Vehículos
class GestionarVehiculosPage extends StatelessWidget {
  const GestionarVehiculosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Gestionar Vehículos')),
      body: const Center(child: Text('CU04 - Gestionar Vehículos')),
    );
  }
}
