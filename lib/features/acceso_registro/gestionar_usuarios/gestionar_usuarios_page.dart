import 'package:flutter/material.dart';

// CU33 - Gestionar Usuarios
class GestionarUsuariosPage extends StatelessWidget {
  const GestionarUsuariosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Gestionar Usuarios')),
      body: const Center(child: Text('CU33 - Gestionar Usuarios')),
    );
  }
}
