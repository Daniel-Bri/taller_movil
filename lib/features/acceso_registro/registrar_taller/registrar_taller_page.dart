import 'package:flutter/material.dart';

// CU12 - Registrar Taller
class RegistrarTallerPage extends StatelessWidget {
  const RegistrarTallerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Registrar Taller')),
      body: const Center(child: Text('CU12 - Registrar Taller')),
    );
  }
}
