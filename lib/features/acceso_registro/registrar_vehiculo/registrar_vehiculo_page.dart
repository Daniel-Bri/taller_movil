import 'package:flutter/material.dart';

// CU03 - Registrar Vehículo
class RegistrarVehiculoPage extends StatelessWidget {
  const RegistrarVehiculoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Registrar Vehículo')),
      body: const Center(child: Text('CU03 - Registrar Vehículo')),
    );
  }
}
